# Core
numpy
opencv-python
Pillow

# Webcam face tracking (--driver webcam / hybrid). The driver uses the
# tasks.FaceLandmarker API (the only API in mediapipe 1.0 / Python 3.13
# wheels); the ~4 MB .task model is downloaded by setup or on first use.
mediapipe>=0.10.9

# OBS virtual camera output (--output virtualcam)
pyvirtualcam

# The real THA3 poser additionally needs PyTorch (install the build matching
# your CUDA version from https://pytorch.org/get-started/locally/) plus a
# checkout of https://github.com/pkhungurn/talking-head-anime-3-demo with its
# model weights. The --mock mode runs with just the packages above.
